[build]
  command = "npm run build"
  publish = "dist"

# Proxy API requests to the production FastAPI backend.
[[redirects]]
  from = "/api/*"
  to = "http://atlas.meteo.gov.gh/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
