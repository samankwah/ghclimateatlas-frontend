[build]
  command = "npm run build"
  publish = "dist"

# Proxy API requests to the FastAPI backend on Vercel
[[redirects]]
  from = "/api/*"
  to = "https://ghclimateatlas-backend.vercel.app/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
